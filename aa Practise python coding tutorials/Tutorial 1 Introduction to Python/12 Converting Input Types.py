# Converting to integer
height_cm = int(input("Height in cm: "))

# Converting to float
weight_kg = float(input("Weight in kg: "))

# Combining input and conversion
temperature = float(input("Temperature: "))


print(height_cm, weight_kg, temperature)